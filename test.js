var ml= [1.2, 1.3, 1.4, 2.0, 2.1, 0.9];

function find(fl, ml){
  
  for ( var i=0 ; i< ml.length ; i++){
    for( var j=1; j< ml.length ; j++){
     // console.log(" "+ ml[i]+" sdf " + ml[j]);
      
      if(ml[i]+ ml[j] === fl  ){
        console.log("true"+ ml[i]+" sdf " + ml[j] + "fl"+ fl);
      }
    }
  }
 
}

function find(fl, ml){
  
  for ( var i=0 ; i< ml.length ; i++){
    for( var j=1; j< ml.length ; j++){
     // console.log(" "+ ml[i]+" sdf " + ml[j]);
      
      if(ml[i]+ ml[j] === fl  ){
        console.log("true"+ ml[i]+" sdf " + ml[j] + "fl"+ fl);
      }
    }
  }
 
}

function find(fl, ml){
  
  for ( var i=0 ; i< ml.length ; i++){
    for( var j=1; j< ml.length ; j++){
     // console.log(" "+ ml[i]+" sdf " + ml[j]);
      
      if(ml[i]+ ml[j] === fl  ){
        console.log("true"+ ml[i]+" sdf " + ml[j] + "fl"+ fl);
      }
    }
  }
 
}

function find(fl, ml){
  
  for ( var i=0 ; i< ml.length ; i++){
    for( var j=1; j< ml.length ; j++){
     // console.log(" "+ ml[i]+" sdf " + ml[j]);
      
      if(ml[i]+ ml[j] === fl  ){
        console.log("true"+ ml[i]+" sdf " + ml[j] + "fl"+ fl);
      }
    }
  }
 
}

function find(fl, ml){
  
  for ( var i=0 ; i< ml.length ; i++){
    for( var j=1; j< ml.length ; j++){
     // console.log(" "+ ml[i]+" sdf " + ml[j]);
      
      if(ml[i]+ ml[j] === fl  ){
        console.log("true"+ ml[i]+" sdf " + ml[j] + "fl"+ fl);
      }
    }
  }
 
}

console.log(find(2.1 , ml))